# ================================
# FimAI Chat 环境变量配置
# ================================

# ================================
# 数据库配置（必需）
# ================================
# SQLite 数据库 (默认，适合开发和小型部署)
DATABASE_URL="file:./prisma/dev.db"

# PostgreSQL 数据库 (推荐用于生产环境)
# 如需使用 PostgreSQL，取消注释下面这行并注释上面的 SQLite 配置:
# DATABASE_URL="postgresql://user:password@localhost:5432/fimai?schema=public"

# ================================
# JWT 认证配置（必需）
# ================================
# 至少 32 字符的随机字符串，用于签名 JWT
# 生成方式: openssl rand -base64 32
# 或使用: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
JWT_SECRET=your-super-secret-jwt-key-at-least-32-characters-long

# JWT 过期时间
JWT_EXPIRES_IN=24h

# ================================
# 加密密钥（推荐配置）
# ================================
# 用于加密存储敏感数据（如 API 密钥）
# 至少 32 字符的随机字符串
# 生成方式: openssl rand -base64 32
# 或使用: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
# ENCRYPTION_KEY=your-encryption-key-at-least-32-characters-long

# ================================
# 应用配置
# ================================
# 运行环境: development, production, test
NODE_ENV=development

# 应用端口
PORT=3000

# 公开的 API 地址（用于前端）
# NEXT_PUBLIC_API_URL=http://localhost:3000

# ================================
# 邮件配置（可选，用于密码重置等功能）
# ================================
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_FROM="FimAI Chat <noreply@fimai.chat>"

# ================================
# 限流配置（可选）
# ================================
# 限流窗口时间（毫秒）
RATE_LIMIT_WINDOW_MS=60000

# 限流窗口内最大请求数
RATE_LIMIT_MAX_REQUESTS=100

# ================================
# 日志配置（可选）
# ================================
# 日志级别: debug, info, warn, error
LOG_LEVEL=info

# 日志文件路径（可选，不设置则只输出到控制台）
# LOG_FILE=logs/app.log
